# Session plan — ship day

**Branch** `bringup-lens-2026-08-14`, continuing from `5708c14` (pushed).
**Started** 2026-08-14 11:28 CST. **Budget:** stop starting new units at 19:30,
handback by 20:00. `date` between units.

The operator is present and reachable for ratifications, so **push is
authorised** on every green unit and `[A-RATIFY]` items ask rather than park.
Everything else in the unattended rules still binds: append-only history,
green-checkpoint commits, Isaac single-occupancy via `/tmp/fleet-isaac.lock`,
`/proc` scans instead of `pgrep` for occupancy, D5 symlink law, gate
discipline, no fleet edits without a narrow grant.

---

## Triage law

The deliverable is four things and nothing else:

1. a watchable end-to-end demonstration run;
2. correction 3's first real speed-error table;
3. a repository whose every claim is true;
4. the evidence pack and the `DELIVERY.md` the email links.

**Cut as of now**, each recorded in `DELIVERY.md` as future work with one
honest line: the ArUco baseline A/B, the matcher A/B, the hardware echo, the
unfilled requalification rows (marked *not measured*, never estimated), the
mast scenery pole. The PDF's XMP metadata was ratified as leave-alone and
stays that way.

## Three deviations from the brief, stated before starting

**1. F0 grows from 30 to 75 minutes and becomes a redesign, not a move.**
The brief's F0 says "lens starts below `simctl start`" as the safe ordering.
That fixes the ordering but not the class: the two blind runs served
`/healthz` = `ok` for their whole length. The operator's condition was "only
fixing the launch would make me confident" and "no more faux launches, even if
that implies writing all over again". So the banner now gates on the lens
having *seen* traffic, not on it answering. ADR 0037.

**2. F1's precondition is already refuted, so F1 takes the brief's own
fallback immediately.** The bench's slip fixture feeds *ground truth* as the
witness — it proves the state machine given a perfect oracle, not that any
real signal exists. No drive-effort or current topic exists anywhere in the
twin, and the laser ε came from a parked-robot regime (ADR 0034). 15 minutes
to state it honestly; the other 45 go to F4.

**3. F3's "full v2 stack" does not exist and cannot be built today.** Two
independent blocks: the v1 estimator returns the *camera's* station, and the
camera is now P's static mast, so it crosses zero gates and produces zero
estimates; and the autonomy plane has no camera at all (the fleet's
`sim_runner.py` is lidar-only and the corridor runner refuses domains 42/43).
The honest bridge is the v1 adapter pointed at the robot1 arena — in
distribution for the detector, real twin, real mast, both planes, sim time —
with **scripted constant-speed passes at A's measured speeds**. The
autonomous profile is evidenced separately on the robot plane (F3.4).
`DELIVERY.md` says this in those words.

---

## Queue

| # | unit | box | status |
|---|---|---|---|
| S0 | This document | 15 m | **DONE** |
| F0 | The banner means seeing (ADR 0037) | 75 m | |
| F1 | DELIVERED_CONFIRMED: take the cut | 15 m | |
| F2 | Speed policy pinned `[A-RATIFY]` | 45 m | |
| F3 | The money runs | 180 m | |
| F4 | U9, the number | 165 m | |
| F5 | The package | 120 m | |
| F6 | Review gate `[A]` | 60 m | |

F2 blocks F3.1. F3.0's probe is a kill criterion for the whole arena path.
F4's offline halves run inside F3's Isaac waits.

## Log

| unit | outcome |
|---|---|
| S0 | this document |

## Handback

*(written at the end; the final scoreboard against the 2026-08-04 interview
corrections, every number traced to a committed artifact)*
