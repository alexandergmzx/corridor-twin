# DERIVED FILE -- do not hand-edit.
# Generated by tools/scale_scenario.py --factor 0.42
# from src/corridor_scene/config/corridor.yaml.
#
# Every length is multiplied by 0.42; every ratio in the
# scenario is therefore unchanged, and so is every geometric argument
# built on it. The supplied drawing has no scale bar, so the authored
# metres were always demo choices -- this simply chooses them for a
# 0.20 x 0.16 m robot instead of for nothing in particular.
#
# NOT scaled: limit_mps (a policy, still un-pinned per ADR 0023),
# angles, pixels, rates, counts, and the robot itself.
schema_version: 0.5.0
status: topology_reconciled_metric_scale_demo_assumption
provenance:
  source_document: docs/ROBO_TASK.pdf
  topology: reconciled_with_supplied_diagram
  metric_scale: demo_assumption
  note: 'The supplied drawing labels the corridor widths only as the symbols m and
    n and carries no scale bar or dimension text. Every metric length below is an
    explicit project choice, not a surveyed value recovered from the diagram. What
    the diagram does fix is topology: one straight corridor face and one tapering
    face, a perpendicular next street, B farther along that street, and P at its corner.'
stage:
  up_axis: Z
  meters_per_unit: 1.0
  default_prim: /World
geometry:
  taper_mode: one_sided_south
  corridor_length_m: 5.04
  building_height_m: 1.68
  wall_thickness_m: 0.21
  west_margin_m: 0.84
  next_street:
    clear_width_m: 2.52
    length_m: 4.2
    turn_radius_m: 0.84
    b_distance_m: 3.36
    b_lateral_fraction: 0.7989
    east_wall_stub:
      depth_fraction: 0.4637
      length_m: 0.4452
      gap_north_of_b_m: 0.441
  profiles:
    nominal_m6_n3:
      entry_width_m: 2.52
      corner_width_m: 1.26
      default: true
    wide_corner_m6_n4_5:
      entry_width_m: 2.52
      corner_width_m: 1.89
    uniform_m6_n6:
      entry_width_m: 2.52
      corner_width_m: 2.52
camera:
  count: 1
  frame_id: p_cam_optical_frame
  width_px: 640
  height_px: 360
  rate_hz: 15.0
  horizontal_fov_deg: 75.0
  mount_height_m: 0.294
fiducials:
  dictionary: DICT_5X5_100
  marker_size_m: 0.168
  first_station_m: 0.84
  spacing_m: 0.84
  wall_plate_cant_deg: 35.0
  references:
    id_base: 80
    plates:
    - surface: north_wall
      along_m: 5.46
      height_m: 0.378
      size_m: 0.252
      cant_deg: 35.0
    - surface: north_wall
      along_m: 6.3
      height_m: 0.84
      size_m: 0.252
      cant_deg: 35.0
    - surface: north_wall
      along_m: 6.678
      height_m: 1.302
      size_m: 0.252
      cant_deg: 35.0
    - surface: east_face
      along_m: -0.252
      height_m: 1.092
      size_m: 0.42
      cant_deg: 0.0
    - surface: east_face
      along_m: 0.021
      height_m: 0.42
      size_m: 0.252
      cant_deg: 0.0
actors:
  b_size_xyz_m:
  - 0.189
  - 0.189
  - 0.714
  a_size_xyz_m:
  - 0.273
  - 0.189
  - 0.21
  landmark_radius_m: 0.063
  landmark_height_m: 0.378
  landmark_offset_m: 0.168
police:
  body_size_xyz_m:
  - 0.252
  - 0.252
  - 0.756
  east_wall_clearance_m: 0.105
  north_offset_m: 0.252
  minimum_clearance_m: 0.105
speed_policy:
  status: demonstration_only
  kind: piecewise_constant_by_clear_width
  rules:
  - maximum_width_m: 1.68
    limit_mps: 0.8
  - maximum_width_m: 2.1
    limit_mps: 1.2
  - maximum_width_m: 420.0
    limit_mps: 1.5
  confidence_sigma: 2.0
  consecutive_estimates: 2
