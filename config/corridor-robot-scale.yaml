# DERIVED FILE -- do not hand-edit.
# Generated by tools/scale_scenario.py --factor 0.2
# from src/corridor_scene/config/corridor.yaml.
#
# Every length is multiplied by 0.2; every ratio in the
# scenario is therefore unchanged, and so is every geometric argument
# built on it. The supplied drawing has no scale bar, so the authored
# metres were always demo choices -- this simply chooses them for a
# 0.20 x 0.16 m robot instead of for nothing in particular.
#
# NOT scaled: limit_mps (a policy, still un-pinned per ADR 0023),
# angles, pixels, rates, counts, and the robot itself.
schema_version: 0.5.0
status: topology_reconciled_metric_scale_demo_assumption
provenance:
  source_document: docs/ROBO_TASK.pdf
  topology: reconciled_with_supplied_diagram
  metric_scale: demo_assumption
  note: 'The supplied drawing labels the corridor widths only as the symbols m and
    n and carries no scale bar or dimension text. Every metric length below is an
    explicit project choice, not a surveyed value recovered from the diagram. What
    the diagram does fix is topology: one straight corridor face and one tapering
    face, a perpendicular next street, B farther along that street, and P at its corner.'
stage:
  up_axis: Z
  meters_per_unit: 1.0
  default_prim: /World
geometry:
  taper_mode: one_sided_south
  corridor_length_m: 2.4
  building_height_m: 0.8
  wall_thickness_m: 0.1
  west_margin_m: 0.4
  next_street:
    clear_width_m: 1.2
    length_m: 2.0
    turn_radius_m: 0.4
    b_distance_m: 1.6
    b_lateral_fraction: 0.7989
    east_wall_stub:
      depth_fraction: 0.4637
      length_m: 0.212
      gap_north_of_b_m: 0.21
  profiles:
    nominal_m6_n3:
      entry_width_m: 1.2
      corner_width_m: 0.6
      default: true
    wide_corner_m6_n4_5:
      entry_width_m: 1.2
      corner_width_m: 0.9
    uniform_m6_n6:
      entry_width_m: 1.2
      corner_width_m: 1.2
camera:
  count: 1
  frame_id: p_cam_optical_frame
  width_px: 640
  height_px: 360
  rate_hz: 15.0
  horizontal_fov_deg: 75.0
  mount_height_m: 0.14
fiducials:
  dictionary: DICT_5X5_100
  marker_size_m: 0.08
  first_station_m: 0.4
  spacing_m: 0.4
  wall_plate_cant_deg: 35.0
  references:
    id_base: 80
    plates:
    - surface: north_wall
      along_m: 2.6
      height_m: 0.18
      size_m: 0.12
      cant_deg: 35.0
    - surface: north_wall
      along_m: 3.0
      height_m: 0.4
      size_m: 0.12
      cant_deg: 35.0
    - surface: north_wall
      along_m: 3.18
      height_m: 0.62
      size_m: 0.12
      cant_deg: 35.0
    - surface: east_face
      along_m: -0.12
      height_m: 0.52
      size_m: 0.2
      cant_deg: 0.0
    - surface: east_face
      along_m: 0.01
      height_m: 0.2
      size_m: 0.12
      cant_deg: 0.0
police:
  body_size_xyz_m:
  - 0.12
  - 0.12
  - 0.36
  east_wall_clearance_m: 0.05
  north_offset_m: 0.12
  minimum_clearance_m: 0.05
speed_policy:
  status: demonstration_only
  kind: piecewise_constant_by_clear_width
  rules:
  - maximum_width_m: 0.8
    limit_mps: 0.8
  - maximum_width_m: 1.0
    limit_mps: 1.2
  - maximum_width_m: 200.0
    limit_mps: 1.5
  confidence_sigma: 2.0
  consecutive_estimates: 2
